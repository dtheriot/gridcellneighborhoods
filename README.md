# gridcellneighborhoods

This application can be verified using the included unit tests. Running `mvn clean install` will execute a test suite that includes the example scenarios provided with the exercise